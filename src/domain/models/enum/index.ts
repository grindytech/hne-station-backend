export enum MediaType {
  Image = 10,
  Video = 20,
  Audio = 30,
}

export enum ViewType {
  Public = 1,
  Private = 0,
}

export enum BlacklistType {
  Yellow = 1,
  Red = 2,
}

export enum Role {
  Admin = 'admin',
  User = 'user',
}
