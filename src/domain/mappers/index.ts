import { DepositMapper } from './deposit.mapper';
import { ProposalMapper } from './proposal.mapper';
import { UserMapper } from './user.mapper';
import { VoteMapper } from './vote.mapper';

export const Mappers = [UserMapper, ProposalMapper, DepositMapper, VoteMapper];
